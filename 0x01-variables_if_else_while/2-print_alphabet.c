#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	char low;

	for (low = 'a'; lo <= '2'; low++)
	{
		putchar(low);
	}
		putchar('\n');

	return (0);
}
